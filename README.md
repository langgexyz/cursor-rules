# Cursor Rules - 团队统一规范

极简规范库，3个核心文件。

## 📦 文件说明

| 文件 | 说明 | 适用范围 |
|------|------|---------|
| `core-rules.mdc` | 核心工作原则 | ✅ 所有项目通用 |
| `dev-rules.mdc` | 开发规范 | ✅ 所有项目通用 |
| `project-spec.template.mdc` | 项目规范模板 | 📝 复制后改名使用 |

## 🚀 使用方法

### 在新项目中使用

```bash
# 1. 复制规范文件到项目根目录（或创建软链接）
ln -s cursor-rules/core-rules.mdc .
ln -s cursor-rules/dev-rules.mdc .

# 2. 复制项目规范模板
cp cursor-rules/project-spec.template.mdc project-spec.mdc

# 3. 编辑项目规范
vim project-spec.mdc  # 修改为你的项目信息

# 4. 创建 .cursorrules 入口文件
cat > .cursorrules << 'EOF'
# Project Name - Cursor AI Rules

通用规范：
- core-rules.mdc
- dev-rules.mdc

项目规范：
- project-spec.mdc
EOF
```

### 简化方式（直接引用）

```bash
# 在 .cursorrules 中直接指定路径
cat > .cursorrules << 'EOF'
# Project Name - Cursor AI Rules

通用规范：
- cursor-rules/core-rules.mdc
- cursor-rules/dev-rules.mdc

项目规范：
- project-spec.mdc
EOF
```

## 📝 规范内容

### core-rules.mdc
- AI工作原则（6大原则）
- Git工作流标准
- Git提交规范
- 代码完整性标准
- 验证标准
- 代码质量标准

### dev-rules.mdc
- Bug排查与验证规范
- 代码修改防遗漏规范
- TypeScript类型安全规范
- React开发规范
- API设计规范
- 测试规范

### project-spec.template.mdc
- 项目概述
- 技术栈配置
- 开发环境
- 业务规则
- 数据结构
- 部署流程

## 🔄 更新规范

当规范有更新时，各项目同步最新版本即可。

## 📌 版本管理

使用 Git 标签管理版本：
```bash
git tag v1.0.0 -m "初始版本"
git tag v1.1.0 -m "新增XXX规范"
```

## 💡 最佳实践

1. **通用规范不要改** - core-rules.mdc 和 dev-rules.mdc 保持一致
2. **项目规范独立维护** - 每个项目的 project-spec.mdc 自己维护
3. **定期同步更新** - 规范有更新时，及时同步到各项目

